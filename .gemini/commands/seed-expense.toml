description = "Seed realistic dummy expenses for a specific user"

prompt = """Read database/db.py to understand the expenses table schema, the db connection pattern, and the database file name.

The command follows this format: '/seed-user <user_id> <count> <months>'

Step 1 — Parse arguments
Parse the <user_id>, <count>, <months> from the input

user_id — integer
count — integer, number of expenses to create
months — integer, how many past months to spread them across
If any argument is missing or not a valid integer, stop and say: "Usage: /seed-expenses <user_id> Example: /seed-expenses 1 50 6"

Step 2 — Verify user exists
Before generating anything, confirm the user_id exists in the users table. If not, stop and say: "No user found with id <user_id>."

Step 3 — Generate and insert expenses
Write and run a Python script(do not save the script, directly run in shell) that:

Spreads expenses randomly across the past months
Uses these categories with realistic Indian descriptions and amounts (₹):
Food: 50–800
Transport: 20–500
Bills: 200–3000
Health: 100–2000
Entertainment: 100–1500
Shopping: 200–5000
Other: 50–1000
Distributes categories roughly proportionally (Food most common, Health and Entertainment least)
Uses the db connection pattern from db.py — do not hardcode the database filename
Uses parameterised queries only — no string formatting in SQL
Inserts all expenses in a single transaction — roll back everything if any insert fails
Step 4 — Confirm
Print:

How many expenses were inserted
The date range they span
A sample of 5 inserted records"""