version: 0.2

phases:
  build:
    commands:
      - echo "Preparing application files for Elastic Beanstalk..."

artifacts:
  files:
    - app.py
    - spendly.db
    - requirements.txt
    - Procfile
    - Dockerfile
    - database/**/*
    - static/**/*
    - templates/**/*